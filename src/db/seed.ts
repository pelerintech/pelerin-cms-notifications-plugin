/**
 * No-op seed file for pelerin_notifications plugin.
 *
 * Rules and templates are admin-created content, not fixtures.
 * This file exists to satisfy the manifest's dbSeed requirement.
 */
export default async function seed() {
  // No seed data — content is created via admin UI.
}
